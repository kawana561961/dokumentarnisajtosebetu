<!doctype html>
<html lang="sr">
	<head>
		<meta charset="UTF-8" />
		<meta name="viewport" content="width=device-width" />
		<link rel="icon" type="image/svg+xml" href="/favicon.svg" />
		<meta name="generator" content={Astro.generator} />
		<title>Šebet - Planinsko selo</title>
		<meta name="description" content="Dokumentarni sajt o malom planinskom selu Šebet" />
	</head>
	<body>
		<header>
			<div class="container">
				<h1>Šebet</h1>
				<nav>
					<ul>
						<li><a href="/">Početna</a></li>
						<li><a href="/istorija">Istorija</a></li>
						<li><a href="/stanovnistvo">Stanovništvo</a></li>
						<li><a href="/tradicija">Tradicija</a></li>
						<li><a href="/galerija">Galerija</a></li>
					</ul>
				</nav>
			</div>
		</header>
		<main>
			<div class="container">
				<slot />
			</div>
		</main>
		<footer>
			<div class="container">
				<p>&copy; {new Date().getFullYear()} Dokumentarni sajt sela Šebet</p>
			</div>
		</footer>
	</body>
</html>

<style is:global>
	:root {
		--color-primary: #2b5329;
		--color-secondary: #8b4513;
		--color-text: #333;
		--color-background: #f5f5f5;
		--color-light: #ffffff;
	}

	html,
	body {
		margin: 0;
		padding: 0;
		font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
		background-color: var(--color-background);
		color: var(--color-text);
		line-height: 1.6;
	}

	.container {
		width: 90%;
		max-width: 1200px;
		margin: 0 auto;
		padding: 0 20px;
	}

	header {
		background-color: var(--color-primary);
		color: var(--color-light);
		padding: 1rem 0;
	}

	header h1 {
		margin: 0;
		font-size: 2.5rem;
	}

	nav ul {
		display: flex;
		list-style: none;
		padding: 0;
		margin: 1rem 0 0 0;
		flex-wrap: wrap;
	}

	nav li {
		margin-right: 1.5rem;
		margin-bottom: 0.5rem;
	}

	nav a {
		color: var(--color-light);
		text-decoration: none;
		font-weight: 500;
		transition: color 0.3s;
	}

	nav a:hover {
		color: #ddd;
		text-decoration: underline;
	}

	main {
		padding: 2rem 0;
		min-height: 70vh;
	}

	footer {
		background-color: var(--color-primary);
		color: var(--color-light);
		padding: 1rem 0;
		text-align: center;
	}

	h1, h2, h3 {
		color: var(--color-secondary);
	}

	img {
		max-width: 100%;
		height: auto;
		border-radius: 5px;
	}

	.hero-image {
		width: 100%;
		height: 400px;
		object-fit: cover;
		margin-bottom: 2rem;
	}

	.gallery {
		display: grid;
		grid-template-columns: repeat(auto-fill, minmax(250px, 1fr));
		gap: 1rem;
		margin: 2rem 0;
	}

	.gallery-item {
		overflow: hidden;
		border-radius: 5px;
		box-shadow: 0 3px 10px rgba(0, 0, 0, 0.2);
		transition: transform 0.3s;
	}

	.gallery-item:hover {
		transform: scale(1.03);
	}

	.gallery-item img {
		width: 100%;
		height: 200px;
		object-fit: cover;
	}

	@media (max-width: 768px) {
		nav ul {
			flex-direction: column;
		}

		nav li {
			margin-bottom: 0.5rem;
		}

		.hero-image {
			height: 250px;
		}
	}
</style>